import { FiltroHtmlPipe } from './filtro-html.pipe';

describe('FiltroHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
